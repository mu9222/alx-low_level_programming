#include <stdio.h>
#include <ctype.h>

/**
 * _isupper - function that checks for uppercase character
 * Return : on upper c = 1 and c = 0 otherwise
 */

int _isupper(int c)
{
	int i = isupper(c);

	return (i);
}
